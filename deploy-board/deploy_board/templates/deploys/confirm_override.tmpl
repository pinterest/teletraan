{% load utils %}
<div class="modal fade" id="confirmOverrideModalId" tabindex="-1" role="dialog"
     aria-labelledby="confirmOverrideModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal"><span
                            aria-hidden="true">&times;</span><span class="sr-only">Close</span>
                    </button>
                    <h4 class="modal-title" id="newEnvModalLabel">Would you like to override?</h4>
                </div>
                <div class="modal-body">
                    <div id="overrideDescription">
                    A current deploy is still ongoing. Are you sure you would like to override the current deploy?
                    </div>
                </div>

                {% if pindeploy_config %}
                {% if pindeploy_config.isPindeploy %}
                <div class="alert alert-warning" role="alert">
                    <strong>Warning!</strong> This stage is managed by Pindeploy. Deployments should be performed using
                    this <a href="{{ pindeploy_config.pipeline }}" target="_blank">pipeline</a>.
                    Creating a deployment from other sources (such as Teletraan) will disable environment management for the pipeline in PinDeploy.
                    This means PinDeploy will stop automatically deploying new builds to the environment in this pipeline.
                    Only proceed this way in case of emergency, and remember to re-enable environment management in PinDeploy once the emergency is resolved.
                </div>
                {% endif %}
                {% endif %}

                <div class="modal-footer">
                    <button id="overrideButton" type="submit" class="btn btn-primary">Override</button>
                    <button id="cancelButton" type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                </div>
            </form>
        </div>
    </div>
</div>
